"use client";

import ThemeComponent from "@/themes";

const ErrorLayout = ({ children }) => {
  return (
    <ThemeComponent>
      ErrorLayout
      {children}
    </ThemeComponent>
  );
};

export default ErrorLayout;
